export const PAGE_ITEMS_NUMBER = 5;

export const ALL_TRANSFERS_FILTER_VALUES = [
  'no-transfers',
  'one-transfer',
  'two-transfers',
  'three-transfers',
];

const settings = {
  PAGE_ITEMS_NUMBER,
  ALL_TRANSFERS_FILTER_VALUES,
};

export default settings;
